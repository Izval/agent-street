{
  "name": "agent-street-keepalive",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker: cron keepalive that pings free-tier services (Render) so they never spin down.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8795 --inspector-port 9345 --local",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260811.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.123.0"
  }
}
