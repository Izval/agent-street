{
  "name": "agent-street-8004-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker: proxy + KV cache a la 8004scan Developer API, con clasificación en 4 categorías.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260811.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.123.0"
  }
}
