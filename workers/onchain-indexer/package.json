{
  "name": "agent-street-onchain-indexer",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker: indexer onchain BSC (balances reales via RPC + precios DexScreener keyless + trades BscScan) que devuelve PortfolioResponse/TradesResponse.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260811.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.123.0"
  }
}
