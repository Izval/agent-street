# Convenience install for the reference orchestrator (see orchestrator/app/agent/pyproject.toml).
# Prefer the editable install described in orchestrator/pyproject.toml.
bnbagent-studio-core>=0.0.5,<0.1
bnbagent>=0.4.0
web3>=7,<8
httpx>=0.27
